# Weapon / medical item models (Poly.pizza, CC0)

Individual CC0 models for melee weapons and medical pickups, mapped in the parent
`items/manifest.json` with `props/` paths. Source: poly.pizza (Public Domain /
CC0 — attribution optional). Some GLBs reference an external palette texture that
isn't shipped; they render with flat material colours (fine for metal/plastic).
